<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.josepmir.germanincontext"
    android:versionName="2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Unlimited German"
        android:theme="@style/AppTheme"
        android:name="com.josepmir.germanincontext.MyApp"
       >



        <activity
            android:name="com.josepmir.germanincontext.MainActivity"
            android:label="Unlimited German"
            android:configChanges="orientation"
            android:screenOrientation="portrait"            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-sdk android:minSdkVersion="14"></uses-sdk>

</manifest>